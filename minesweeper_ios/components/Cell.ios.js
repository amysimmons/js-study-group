var React = require('react-native');

var {
  StyleSheet,
  Text,
  View,
} = React;

var Cell = React.createClass({
  render: function() {
    return (
      <View>
      	<Text>Cell</Text>
      </View>
    );
  }
});

export default Cell;